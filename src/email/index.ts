/**
 * Public exports for the email helpers used by this Keycloak theme.
 * Keeps imports concise elsewhere in the project (e.g. `import { EmailLayout, getMessages } from './email'`).
 */
export { getMessages } from "./i18n";
export { EmailLayout } from "./layout";
export { default as defaultEmailLayout } from "./layout";
