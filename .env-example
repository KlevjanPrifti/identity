# ============================================================================
# Keycloak Version & Base Configuration
# ============================================================================
KEYCLOAK_VERSION=26.4.7
KEYCLOAK_PORT=8080

# ============================================================================
# Keycloak Runtime Configuration
# ============================================================================
# Hostname - change to your actual domain in production (e.g., auth.yourdomain.com)
KC_HOSTNAME=localhost

# Feature flags (these match what's in the Dockerfile build stage)
KC_HEALTH_ENABLED=true
KC_METRICS_ENABLED=true
KC_HTTP_ENABLED=true
KC_PROXY=edge

# ============================================================================
# PostgreSQL Database - SENSITIVE DATA
# ============================================================================
# PostgreSQL container configuration
POSTGRES_DB=keycloak-custom
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=changeme-strong-password-here

# Keycloak database connection (runtime only - NOT used during build)
KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak-custom
KC_DB_USERNAME=keycloak
KC_DB_PASSWORD=changeme-strong-password-here

# Connection pool settings (optional tuning)
KC_DB_POOL_INITIAL_SIZE=50
KC_DB_POOL_MIN_SIZE=50
KC_DB_POOL_MAX_SIZE=50
QUARKUS_TRANSACTION_MANAGER_ENABLE_RECOVERY=true

# ============================================================================
# Keycloak Admin User - SENSITIVE DATA
# ============================================================================
# Bootstrap admin credentials (used on first startup)
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=changeme-strong-password-here